import { AlertCircle, Inbox, LoaderCircle } from "lucide-react"
import { Button } from "@/components/ui/button"
import { Card, CardContent } from "@/components/ui/card"

export function SocialImpactLoading() { return <div aria-label="Loading social impact data" className="grid gap-4 md:grid-cols-2 lg:grid-cols-4">{[0,1,2,3].map((item) => <div key={item} className="h-32 animate-pulse rounded-xl bg-muted" />)}<span className="sr-only"><LoaderCircle />Loading</span></div> }
export function SocialImpactError({ message, onRetry }: { message: string; onRetry: () => void }) { return <Card role="alert"><CardContent className="flex flex-col items-center py-10 text-center"><AlertCircle className="mb-3 h-10 w-10 text-destructive" /><h2 className="font-semibold">Social impact data could not be loaded</h2><p className="mt-1 text-sm text-muted-foreground">{message}</p><Button onClick={onRetry} className="mt-4">Try again</Button></CardContent></Card> }
export function SocialImpactEmpty({ filtered, onClear }: { filtered?: boolean; onClear?: () => void }) { return <Card><CardContent className="flex flex-col items-center py-10 text-center"><Inbox className="mb-3 h-10 w-10 text-muted-foreground" /><h2 className="font-semibold">{filtered ? "No ventures match these filters" : "No social impact data yet"}</h2><p className="mt-1 text-sm text-muted-foreground">{filtered ? "Try changing or clearing your filters." : "Add portfolio ventures before reviewing social impact."}</p>{filtered && onClear && <Button variant="outline" onClick={onClear} className="mt-4">Clear filters</Button>}</CardContent></Card> }
