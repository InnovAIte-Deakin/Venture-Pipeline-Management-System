import { Mail, MapPin, Phone } from "lucide-react"
import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle } from "@/components/ui/dialog"
import { getKycStatusBadge, getLimitedPartnerStatusBadge } from "@/lib/fund-management/status"
import type { LimitedPartner } from "@/types/fund-management"

export function LimitedPartnerDialog({ open, onOpenChange, limitedPartner }: { open: boolean; onOpenChange: (open: boolean) => void; limitedPartner: LimitedPartner | null }) {
  return <Dialog open={open} onOpenChange={onOpenChange}><DialogContent className="max-h-[90vh] w-[95vw] max-w-3xl overflow-y-auto"><DialogHeader><DialogTitle className="break-words">{limitedPartner?.name ?? "Limited partner"}</DialogTitle><DialogDescription>Investor details, performance, and compliance</DialogDescription></DialogHeader>{limitedPartner && <div className="grid gap-6 sm:grid-cols-2"><section className="space-y-3"><h3 className="font-semibold">Investment</h3>{[["Commitment", limitedPartner.commitment], ["Called", limitedPartner.called], ["Distributed", limitedPartner.distributed], ["NAV", limitedPartner.nav], ["IRR", `${limitedPartner.irr.toFixed(1)}%`], ["TVPI", `${limitedPartner.tvpi.toFixed(2)}x`]].map(([label, value]) => <div key={label} className="flex justify-between gap-4 text-sm"><span className="text-muted-foreground">{label}</span><span className="break-words text-right font-medium">{value}</span></div>)}</section><section className="min-w-0 space-y-3"><h3 className="font-semibold">Contact & compliance</h3><div className="flex items-start gap-2 text-sm"><Mail className="mt-0.5 h-4 w-4 shrink-0" /><span className="break-all">{limitedPartner.email}</span></div><div className="flex items-center gap-2 text-sm"><Phone className="h-4 w-4 shrink-0" />{limitedPartner.phone}</div><div className="flex items-center gap-2 text-sm"><MapPin className="h-4 w-4 shrink-0" />{limitedPartner.country}</div><div className="flex flex-wrap gap-2">{getLimitedPartnerStatusBadge(limitedPartner.status)}{getKycStatusBadge(limitedPartner.kycStatus)}</div></section></div>}</DialogContent></Dialog>
}
